#include <Geode/Geode.hpp>
#include <Geode/modify/GJBaseGameLayer.hpp>

using namespace geode::prelude;

//  AUN EN PROGRESO, FALTARA AGREGAR LOS IDS DE LOS ICONOS NUEVOS, PERO ESO ES PEO DE IZUMI AHORA
/*
class $modify(OdysseyBaseGameLayer, GJBaseGameLayer)
{
    void update(float delta)
    {
        GJBaseGameLayer::update(delta);


        m_effectManager->updateCountForItem(5001, GameManager::sharedState()->getPlayerFrame());
        updateCounters(5001, GameManager::sharedState()->getPlayerFrame());
    }
};
*/